# GridFlow AI - Web & Backend Placeholder

## Overview
This directory will contain the Next.js application for the marketing site and backend API (Serverless).
It connects to Supabase for user data.

## Setup
To initialize later:
```bash
npx create-next-app@latest . --typescript --tailwind --eslint
```
